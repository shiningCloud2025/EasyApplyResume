<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zyh.easyapplyresume.mapper.mysql.user.UserMapper">
    <select id="findByAccountOrPhoneOrEmail" resultType="com.zyh.easyapplyresume.model.pojo.user.User">
        select * from user_user
        where user_account = #{accountOrPhoneOrEmail} or user_phone = #{accountOrPhoneOrEmail} or user_email = #{accountOrPhoneOrEmail}
    </select>

</mapper>