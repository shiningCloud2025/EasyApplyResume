# 管理端用户头像和用户名显示修复

## 问题描述
用户登录管理端后，右上角显示的账号名和头像是写死的默认值，没有显示真实的用户信息。

## 问题原因
1. 后端 `/admin/auth/getAdminInfo` 接口返回的是 `SecurityUser` 对象，只包含基本字段（userId, username, authorities等）
2. 前端需要显示的 `adminImage`（头像）和 `adminUsername`（用户名）等详细信息不在 `SecurityUser` 中
3. 前端登录后没有获取完整的管理员信息

## 解决方案
参考个人中心的实现，在获取 `SecurityUser` 后，使用其中的 `userId` 调用 `/admin/admin/findById` 接口获取完整的管理员信息。

## 修改的文件

### 1. app/admin/src/store/auth.ts
**修改内容：**
- 更新 `AdminUser` 接口定义，添加 `userId`、`roleInfoVOS` 和 `authorities` 字段
- 修改 `getUserInfo()` 方法：
  - 先调用 `/admin/auth/getAdminInfo` 获取 `SecurityUser`（包含 userId）
  - 再调用 `/admin/admin/findById` 获取完整的管理员信息
  - 合并两个对象的数据存储到 `state.user` 中，确保 `userId` 和 `adminId` 都存在
- 更新 `userRoles` getter，优先使用 `roleInfoVOS` 字段

**关键代码：**
```typescript
// 获取用户信息
async getUserInfo() {
  try {
    // 先获取基本的SecurityUser信息（包含userId）
    const response = await api.post<any>('/admin/auth/getAdminInfo')
    
    if (response.data?.userId) {
      // 使用userId获取完整的管理员信息
      const adminResponse = await api.get<AdminUser>(`/admin/admin/findById?adminId=${response.data.userId}`)
      
      // 合并SecurityUser和AdminUser信息
      this.user = {
        ...adminResponse.data,
        authorities: response.data.authorities
      } as AdminUser
      
      return this.user
    }
  } catch (error) {
    console.error('获取用户信息失败:', error)
    return null
  }
}
```

### 2. app/admin/src/layout/AdminLayout.vue
**修改内容：**
- 在组件 `onMounted` 生命周期中添加用户信息获取逻辑
- 检查如果已登录但没有用户信息，则自动调用 `getUserInfo()`

**关键代码：**
```typescript
onMounted(async () => {
  if (!authStore.isLoggedIn) {
    router.push('/login')
    return
  }
  
  // 如果已登录但没有用户信息，则获取用户信息
  if (!authStore.user) {
    await authStore.getUserInfo()
  }
})
```

## 测试步骤
1. 清除浏览器缓存和 localStorage
2. 访问管理端登录页面 `http://localhost:xxxx/login`
3. 使用任意登录方式（账号密码/手机验证码/邮箱验证码）登录
4. 登录成功后，查看右上角：
   - 头像应该显示真实的管理员头像（adminImage）
   - 用户名应该显示真实的管理员姓名（adminUsername）
5. 点击右上角的下拉菜单，选择"个人中心"
6. 验证个人中心页面显示的信息是否正确

## 涉及的接口
1. `POST /admin/auth/getAdminInfo` - 获取当前登录用户的基本信息（SecurityUser）
2. `GET /admin/admin/findById?adminId={id}` - 根据管理员ID获取完整的管理员信息

## 数据流程
```
用户登录
  ↓
authStore.login() 
  ↓
authStore.getUserInfo()
  ↓
POST /admin/auth/getAdminInfo (获取 SecurityUser，包含 userId)
  ↓
GET /admin/admin/findById?adminId={userId} (获取完整信息)
  ↓
合并数据存储到 authStore.user
  ↓
AdminLayout 组件渲染时读取 authStore.user
  ↓
显示真实的头像和用户名
```

## 注意事项
1. 如果页面刷新后用户信息丢失，`AdminLayout` 组件会在挂载时自动重新获取
2. 所有登录方式（账号密码、手机验证码、邮箱验证码）都会在登录成功后自动调用 `getUserInfo()`
3. 如果获取用户信息失败，不会影响登录流程，只是用户信息显示为默认值

## 相关文件
- `app/admin/src/store/auth.ts` - 认证状态管理
- `app/admin/src/layout/AdminLayout.vue` - 管理端布局组件
- `app/admin/src/views/Profile.vue` - 个人中心页面（参考实现）
- `src/main/java/com/zyh/easyapplyresume/controller/admin/AdminAuthController.java` - 后端认证控制器
- `src/main/java/com/zyh/easyapplyresume/controller/admin/AdminController.java` - 后端管理员控制器
- `src/main/java/com/zyh/easyapplyresume/security/SecurityUser.java` - Security用户类

